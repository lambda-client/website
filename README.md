# Lambda website

Static site for [lambda-client](https://github.com/lambda-client).

```bash
npm install
npm run dev     # rebuild style.css on change
npm run build   # minified build, run before committing
```
